interface:
  display_name: "Guardlink"
  short_description: "Repo-specific patterns and workflows for guardlink"
  default_prompt: "Use the guardlink repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true